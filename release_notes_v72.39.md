## v72.39

- เปลี่ยนปุ่มขายของแบบเร็วเป็น `ซื้อ Blue Herb` พร้อมช่องตั้งจำนวนเป้าหมายในคลัง
- ใช้ `ซื้อไอเทม.png` ยืนยันหน้าร้าน NPC และค้นหา `Blue Herb.png` เฉพาะขอบเขตกระเป๋าที่กำหนด
- ซื้อครั้งละ 500 ชิ้น ฝากเข้าคลังหน้า 1 แล้วอ่านจำนวนหน้า `Blue Herb` จนถึงเป้าหมาย
- รองรับหัวหน้าต่าง Kafra ภาษาไทยด้วย `bcc_3` และใช้ชุดอ่านตัวเลขเดิมของโฟลว์ลูกธนู
- หากเปิดคลังค้างและจำนวนถึงเป้าแล้ว จะปิดคลังและจบโดยไม่ซื้อซ้ำ
- ปรับการยืนยัน `bcc_2`: ขณะยังพบภาพให้กด Space แบบเร็ว ตรวจภาพใหม่ทุก 200 ms และหยุดกดทันทีเมื่อภาพหาย
- รอ `bcc_3` ทุก 150 ms และยังคง timeout รวม 60 วินาที
- โฟลว์ซื้อ ฝาก และอ่านจำนวนทดสอบกับจอจริงแล้ว; การปรับ Space gate ล่าสุด Build ผ่านและรอผู้ใช้ทดสอบบนเครื่อง Xeon

SHA256: `5C0333CF916B4CEC4C3D9CC58F19EBC95B9317B1AF508AC1126D79709C0021A4`
