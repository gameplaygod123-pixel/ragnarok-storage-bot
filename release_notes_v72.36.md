## v72.36

- เพิ่มตัวเลือก `เก็บของเมื่อค่าน้ำหนักเป็นสีแดง` สำหรับโฟลว์เก็บของเข้าคลัง
- ใช้ตัวตรวจสีแดงบริเวณน้ำหนักแบบเดียวกับโฟลว์ขายของ (`red pixels >= 5`)
- ถ้าน้ำหนักยังไม่แดง จะข้ามการเปิด Kafra/ฝากของ และกลับไปทำงานต่อ
- ถ้าน้ำหนักแดง จะเปิด Kafra และฝากของตามหน้าที่เลือกไว้ตามปกติ
- ระบบตรวจ popup เริ่มต้น, เกมหลุด, Kafra ค้าง และการกลับไปทำงานต่อยังทำงานตามเดิม
- เมื่อเลือกเติม Blue Herb บอทจะตรวจและทำ Blue Herb ก่อนตรวจน้ำหนักเหมือนเดิม
- จำสถานะ checkbox ใน settings แบบเข้ารหัส
- เพิ่ม timeout โฟลว์ปุ่มขายของจาก 30 เป็น 60 วินาที รองรับเครื่อง Xeon/VM ที่ตอบสนองช้า

SHA256: `F35C5669B4FB9C8D8089C37E8F0241D24A2E9E9837C92E33FA69794271C0E2EE`
