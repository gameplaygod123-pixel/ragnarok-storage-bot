## v72.28

- เปลี่ยนปุ่มยืนยันตัวเลือก `Sell` ในหน้าสนทนา NPC จาก `Enter` เป็น `Spacebar`
- ยังคงตรวจ `arrow_slot1_3.png` เพื่อยืนยันว่าหน้าร้านเปิดแล้ว
- จุดยืนยันจำนวนยังใช้ Enter ครั้งเดียวแบบรอตามภาพจาก v72.27
- `v72.27` ยังอยู่บน GitHub สำหรับย้อนกลับ

SHA256: `6F865B52CE8090F20B07713C99E7D3C4F96251A1EF3D6C51A01B795A073BE747`
