## v72.59

- เพิ่มช่องเลือกชนิดซองธนูสำหรับปุ่ม `ไปธนู`: `Quiver` และ `Fire Arrow Quiver`
- เลือกได้ทีละชนิดและใช้ `Quiver` เป็นค่าเริ่มต้น
- บันทึกตัวเลือกไว้ใน settings เพื่อใช้ครั้งถัดไป
- โฟลว์ถอนซองธนูให้ครบ 110 จะค้นหาและลากชนิดที่เลือกเท่านั้น
- ใช้ OCR อ่านชื่อเต็มรายแถว ป้องกัน `Quiver` จับผิดเป็นท้ายชื่อ `Fire Arrow Quiver`
- ทดสอบ read-only จากจอเกมจริง: พบ `Quiver` แถว 3 และ `Fire Arrow Quiver` แถว 5 ถูกต้อง โดยไม่ส่ง input
- Build ผ่าน; มี warning async เดิม 2 จุด

SHA256: `29D74D4C4BDDC7D46525AE3EF37AC32E9EF79835C5812AA76CCCBFE65FC657F6`
