## v72.42

- แก้จอเกมที่เปิดใหม่ค้างหน้า `ข้อกำหนดการให้บริการ` (`acc_7`) ก่อน Login
- ปรับจุดกดปุ่มยอมรับจาก offset เดิม `(+235,+265)` เป็นตำแหน่งจากภาพจริง `(+235,+330)`
- ใช้จุดใหม่นี้ทั้ง popup ช่วงเริ่มต้นและกรณีพบ `acc_7` ล่าช้า
- RUN ALL ตรวจ `acc_7`, `acc_8`, `acc_10` แล้ว route จอเดิมเข้า saved-ID Login ก่อนทำโฟวคลัง
- เพิ่ม fallback จับคู่จอจากลำดับ login slot 1–3 เมื่อ PID/start-time mapping หลุดชั่วคราว
- ป้องกันหน้าก่อน Login ถูกส่งเข้า storage flow แบบไม่มี automatic reconnect
- ต้องติ๊กเปิดใช้และกรอกข้อมูล Login ของแถวนั้นครบ จึงจะล็อกอินอัตโนมัติ
- Build ผ่าน; Blue Herb image regression จาก v72.41 ยังผ่าน 15/15
- ยังไม่ได้ทดสอบกด `acc_7` กับจอเกมจริงหลังแก้

SHA256: `C1D60239DA6B12C58EF13BA70871C5E1FC7E005C9294F317F0476867F563B1C7`
