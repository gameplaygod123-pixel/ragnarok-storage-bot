## v72.34

- แก้โฟลว์ขายของบนเครื่อง Xeon/VM ที่ค้างหน้าเลือกบริการ Kafra จนครบ 30 วินาที
- หลังพบ `bcc_2` จะตรวจภาพเมนูอีกครั้งก่อนส่งปุ่มยืนยันทุกครั้ง
- เปลี่ยนจาก Space แบบเร็ว 55 ms เป็นปุ่มแบบปกติ 180 ms
- สลับ Space และ Enter เป็นทางสำรองเมื่อเมนูยังไม่หาย
- รอ 500 ms หลังแต่ละปุ่ม รองรับเครื่องที่ตอบสนองหรือวาดหน้าคลังช้า
- เมื่อ `bcc_2` หาย จะหยุดส่งปุ่มและรอ `inventory_open_marker + bcc_3` ป้องกันการกดแทรกระหว่างเปลี่ยนหน้า
- รวมการแก้จุดคลิกขายฝั่งซ้ายและตัวอ่านราคาจาก v72.33
- `v72.33` ยังอยู่บน GitHub สำหรับย้อนกลับ

SHA256: `C9C2CAEABD06190DD26C714ADBE048BF6F23A2A1F8A70753D3C43534AF0C7897`
