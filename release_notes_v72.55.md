## v72.55

- เวทรองรับทั้ง C1 และ C2 ในโฟลว์เดียว
- หน้าเลือกตัวเวทตรวจ `เลือกตัวเวทC2.png` และ `เลือกตัวเวทC1.png`
- หลังเข้าเกมเวทตรวจ `ในเกมเวทC2.png` และ `ในเกมเวทC1.png`
- เปลี่ยนชื่อรูปเวทเดิม `ccx_1/ccx_2` เป็นชื่อ C2 ที่ชัดเจน โดยไม่เปลี่ยน `ccx.png` ของหน้าซัพพาส
- ปรับหัวข้อ `ฟามด้วง` ให้เลือก checkbox มอนแถว 2 (`Beetle King`) เพียงแถวเดียว จากเดิมเลือกแถว 2 และ 4
- ย้ายปุ่ม `นอนอ้วน ♡` จากด้านล่างขึ้นแถวบนของหน้า Login ก่อนปุ่มเรียงจอ โดยคง event และการทำงานเดิม
- Build ผ่านและตรวจ resource/mapping ครบ

SHA256: `8480CD37651604723639A2BAFEEEC5CED0BB1F5C53D5E42DFFDAEFE57843D43D`
