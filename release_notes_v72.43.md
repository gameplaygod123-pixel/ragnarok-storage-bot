## v72.43

- คืนตำแหน่งกดปุ่ม `ยอมรับ` หน้า `ข้อกำหนดการให้บริการ (acc_7)` เป็นระบบเดิมที่พิสูจน์แล้ว `acc_7.Center + (235,265)`
- คืนทั้งจุดตรวจ popup ตอนเริ่ม Login และจุดตรวจ popup ที่ปรากฏล่าช้า
- ภาพจริงจาก `CaptureClient` ของบอทบนจอเกมลำดับ 2 แสดงปุ่มยอมรับประมาณ `(616,520)` ซึ่งตรงกับ offset เดิม
- แก้ regression ของ v72.42 ที่เปลี่ยนแกน Y เป็น `+330` และกดต่ำเกินไปที่ประมาณ `(616,585)`
- คงระบบ RUN ALL pre-Login recovery และ fallback จับคู่ login slot 1–3 จาก v72.42
- หน้าเลือก `Europe / Southeast Asia (acc_8/acc_10)` ไม่ถูกเปลี่ยน และตรวจจากภาพบอทแล้วว่าจุดเลือก/ยืนยันเดิมตรง
- Build ผ่าน; Blue Herb image regression ยังผ่าน 15/15
- ผู้ใช้จะทดสอบ Login/RUN ALL กับจอจริง

SHA256: `BF126040ADEA330C63246AE5A4EB3EF02F4F007C91B2161E26FB81DF64ED0332`
